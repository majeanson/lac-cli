# Exclude everything by default, then whitelist what's needed
**
# Explicitly exclude parent workspace traversal (symlinks in node_modules resolve to ../)
../**
!out/extension.js
!package.json
!readme.md
!icon.png
